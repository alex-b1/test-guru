<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('element.link.show'), admin_question_path(question) %></td>
  <td><%= link_to t('element.link.edit'), edit_admin_question_path(question) %></td>
  <td><%= link_to t('element.link.delete'),
                  admin_question_path(question),
                  method: :delete,
                  data: {confirm: t('element.popup.confirm')} %>
  </td>
</tr>